# CloudVault Alembic configuration.
#
# Run migrations from this directory:
#   alembic upgrade head
#
# Or from the repository root:
#   alembic -c database/migrations/alembic.ini upgrade head
#
# The DATABASE_URL is resolved in env.py: the DATABASE_URL environment
# variable takes precedence, otherwise the application settings are used
# (which read backend/.env or the repository-root .env file).

[alembic]
script_location = %(here)s
file_template = %%(rev)s_%%(slug)s
prepend_sys_path = .
path_separator = os

[post_write_hooks]

[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARN
handlers = console
qualname =

[logger_sqlalchemy]
level = WARN
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S
